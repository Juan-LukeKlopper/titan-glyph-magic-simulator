pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: npx eslint {staged_files}
      stage_fixed: true
    typecheck:
      run: npx tsc --noEmit
    test:
      run: npx vitest run
